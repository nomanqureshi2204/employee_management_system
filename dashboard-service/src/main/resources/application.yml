server:
  port: 8085

spring:
  application:
    name: DASHBOARD-SERVICE

eureka:
  client:
    service-url:
      # 🔥 FIX: CI/Docker me localhost kaam nahi karega
      # eureka-server = Docker service name
      defaultZone: http://eureka-server:8761/eureka/

    register-with-eureka: true
    fetch-registry: true